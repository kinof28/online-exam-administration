import { ClientHttpInterceptor } from './client-http-interceptor';

describe('ClientHttpInterceptor', () => {
  it('should create an instance', () => {
    expect(new ClientHttpInterceptor()).toBeTruthy();
  });
});
